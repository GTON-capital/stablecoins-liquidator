apiVersion: v1
kind: Service
metadata:
  name: {{ include "liquidator.fullname" . }}
  labels:
    {{- include "liquidator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liquidator.selectorLabels" . | nindent 4 }}
